<UserControl x:Class="SyslabControlAlgorithmFrameworkWpfGui.View.ScadaUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ViewModel="clr-namespace:SyslabControlAlgorithmFrameworkWpfGui.ViewModel"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SyslabControlAlgorithmFrameworkWpfGui.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.DataContext>
        <ViewModel:ScadaViewModel />
    </UserControl.DataContext>
    <UniformGrid>
        <GroupBox Header="Previous Requests 1">
            <ListView ItemsSource="{Binding PreviousRequests1}"/>
        </GroupBox>
        <GroupBox Header="Previous Requests 2">
            <ListView ItemsSource="{Binding PreviousRequests2}"/>
        </GroupBox>
        <GroupBox Header="Previous Requests 3">
            <ListView ItemsSource="{Binding PreviousRequests3}"/>
        </GroupBox>
        <GroupBox Header="Previous Requests 4">
            <ListView ItemsSource="{Binding PreviousRequests4}"/>
        </GroupBox>
    </UniformGrid>
</UserControl>
